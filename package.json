{
  "scripts": {
    "build": "sass static/scss:static/css",
    "watch": "sass static/scss:static/css --watch",
    "flask": "python3 app.py",
    "watch-reload": "nodemon --watch static/css --watch templates/ --watch . --ext html,css --exec \"echo 'Files changed, refresh browser'\"",
    "dev": "npm-run-all --parallel  flask watch"
  },
  "devDependencies": {
    "sass": "^1.85.1",
    "nodemon": "^3.0.2",
    "npm-run-all": "^4.1.5"
  }
}
